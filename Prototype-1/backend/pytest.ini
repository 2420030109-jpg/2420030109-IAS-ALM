[pytest]
# Run from Prototype-1/backend/ :  venv/Scripts/python.exe -m pytest -q
# `pythonpath` puts the backend package modules AND the tests helper module on
# sys.path; `testpaths` points at the sibling tests/ directory.
testpaths = ../tests
pythonpath = . ../tests
